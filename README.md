# DevOps NGINX Demo

## Intro

# This script helps you to install nginx on linux and then show demo page

## Steps

sudo apt-get update
sudo apt install nginx

sudo systemctl start nginx
sudo systemctl enable nginx

Webpage is kept at /var/www/html
